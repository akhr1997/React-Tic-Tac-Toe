/**
 * The public folder is available to "public", so we can access outside the code as well
 * But the src folder is bundeled.
 */

/**
 * Should not directly update the [] and {} in setstate function
 * rather create a copy of it and change in a immutable way
 */

/**
 * I we have to send arguments in onClick methos, we gotta send as a callback function
 * onCLikc = {() => handleClick(arg2, arg2)}
 */

/**
 * Things covered:
 *      Player Dashboard - Just a component
 */
